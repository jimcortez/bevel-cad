"""
Bundled example parts.

Each module exposes ``build(cfg)`` decorated with :func:`bevel_cad.part` and is
registered under the ``bevel_cad.parts`` entry-point group, so
``bevel render planet_spacer`` works from any directory.
"""
